#include "main.h"

/**
 * print _last_digit - prints the last digit of a numebr
 * @n: the number to be treated
 * Return: value of the last digit
 */

int print_last_digit(int n)
{
	int ld;

	ld = n % 10;
	if (last < 0)
	{
		ld = ld * -1;
	}
}-putchar(last + '0');
	return (

